version: 2

build:
  image: latest

sphinx:
  fail_on_warning: true

python:
  version: 3.7
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
      extra_requirements:
        - docs

formats: []
